<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Products</title>
  </head>
  <body>
    <h1>All products!</h1>

    <ul>
      <% products.forEach(product => { %>
      <li><a href="products/<%= product.id %>">Product name: <%= product.name %>, Price: <%= product.price %>, Category: <%= product.category %></a></li>
      <% }) %>
    </ul>
  </body>
</html>
